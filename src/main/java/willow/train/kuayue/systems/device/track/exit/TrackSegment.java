package willow.train.kuayue.systems.device.track.exit;

import kasuga.lib.core.create.boundary.CustomTrackSegment;

import java.util.UUID;

public class TrackSegment extends CustomTrackSegment {
    public TrackSegment(UUID segmentId) {
        super(segmentId);
    }
}
